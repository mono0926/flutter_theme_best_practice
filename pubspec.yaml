name: theme_best_practice
description: A new Flutter application.
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0 <3.0.0"
dependencies:
  cupertino_icons:
  enum_to_string:
  fl_chart:
  flutter:
    sdk: flutter
  mono_kit:
  nested:
  provider:
  recase:
dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono:
flutter:
  uses-material-design: true
